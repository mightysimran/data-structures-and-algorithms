#include<bits/stdc++.h>
using namespace std;

int main(){

    // list<int> ls = {1, 2};

    // ls.push_front(0); // {0, 1, 2}
    // ls.push_back(3); // {0, 1, 2, 3} 

    // for(auto i: ls){
    //     cout << i << " ";
    // }

    // rest fncs same as vector
    // begin, end, rbegin, rend, clear, insert, size, swap

    return 0;
}