#include <bits/stdc++.h>
using namespace std;

int main(){

    // map<int, string> m; 

    // m[1] = "abc";
    // m[3] = "lmn";
    // m.insert({2, "def"});

    // map<int, string>::iterator i;
    // for (i = m.begin(); i != m.end(); i++){
    //     cout << (*i).first << " " << (*i).second << endl;
    // }

    // for(auto &i: m){
    //     cout << i.first << " " << i.second << endl;
    // }

    return 0;
}