#include<bits/stdc++.h>
using namespace std;

int main(){
    
    // vector<int> v;

    // v.push_back(1);
    // v.emplace_back(2);

    // vector<pair<int,int>> v;
    // v.push_back({1,2});
    // v.emplace_back(1,2);

    // vector<int> v(5,100); // {100,100,100,100,100}

    // vector<int> first(5,25); // {25,25,25,25,25}
    // vector<int> second(first);

    return 0;
}